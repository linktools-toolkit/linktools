name: Temporary unified release gates validation

on:
  push:
    branches:
      - refactor/unified-release-gates
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  diagnostic-python36-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Run compatibility scanner
        run: |
          python scripts/check/python36.py \
            manage.py \
            scripts/check/python36.py \
            linktools/src \
            linktools-common/src \
            linktools-mobile/src \
            linktools-cntr/src

  diagnostic-ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install Ruff
        run: python -m pip install 'ruff>=0.16,<0.17'
      - name: Run Ruff
        run: |
          ruff check --no-cache \
            scripts/check/ai \
            linktools-ai/src/linktools/ai \
            linktools-ai/src/linktools/commands/ai \
            tests/ai

  diagnostic-pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install -U -r requirements.txt
          python manage.py install --editable
      - name: Run pytest
        run: python -m pytest -q -p no:cacheprovider tests

  python310:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install -U -r requirements.txt
          python manage.py install --editable
      - name: Run source gates
        run: python manage.py check
      - name: Build release artifacts
        run: VERSION=v0.0.0 RELEASE=true python manage.py build
      - name: Verify release artifacts
        run: VERSION=v0.0.0 python manage.py verify
      - name: Verify legacy paths are absent
        run: |
          ! git grep -n 'scripts\.build' -- .
          ! git grep -n 'linktools-ai/scripts/build' -- .
          ! git grep -n 'scripts/build/matrix' -- .

  python36:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.6'
      - name: Install Python 3.6 launcher dependency
        run: python -m pip install 'tomlkit==0.11.8'
      - name: Compile Python 3.6 tooling
        run: |
          python -m py_compile manage.py
          python -m py_compile scripts/check/python36.py
      - name: Run Python 3.6 compatibility scanner
        run: |
          python scripts/check/python36.py \
            manage.py \
            scripts/check/python36.py \
            linktools/src \
            linktools-common/src \
            linktools-mobile/src \
            linktools-cntr/src
      - name: Verify AI interpreter fail-fast
        shell: bash
        run: |
          set +e
          output="$(python manage.py check linktools-ai 2>&1)"
          status=$?
          set -e
          printf '%s\n' "$output"
          test "$status" -ne 0
          printf '%s\n' "$output" | grep -F 'Python 3.10 is required by linktools-ai; current interpreter is 3.6'
