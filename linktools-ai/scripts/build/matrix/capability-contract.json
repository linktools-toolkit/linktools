{
  "registration_order": ["AgentSpec", "PromptSpec", "SkillSpec", "MCPServerSpec"],
  "capabilities": ["mcp", "skill", "retrieval"],
  "production_rules": [
    "resolver registry is immutable after composition",
    "required capability resolution is fail-closed",
    "no local fallback for production",
    "MCP toolsets enter Pydantic AI through Toolset capabilities"
  ]
}
