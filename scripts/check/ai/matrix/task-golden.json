{
  "graph": {
    "graph_id": "review",
    "nodes": [
      {"task_id": "load", "dependencies": []},
      {"task_id": "inspect", "dependencies": ["load"]},
      {"task_id": "report", "dependencies": ["inspect"]}
    ],
    "topological_order": ["load", "inspect", "report"]
  },
  "rules": ["duplicate node rejected", "unknown dependency rejected", "cycle rejected", "production execution delegates to durable workflow"]
}
